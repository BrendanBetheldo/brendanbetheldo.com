// Open-Meteo fetch + geolocation utility — implementation TBD

export type WeatherState = 'clear' | 'cloudy' | 'rain' | 'storm' | 'snow';

export function normaliseWindSpeed(_kmh: number): number {
  return 0;
}
